name: WORKFLOW_FAILURE_ALERT

# Emails Joe whenever any scheduled workflow finishes in a failure state.
# 2026-07-06: transport switched Resend -> Gmail SMTP (same SMTP_USER /
# SMTP_PASSWORD secrets the daily-brief pipeline sends with every day).
# Resend returned HTTP 403 on BOTH send attempts ever made (2026-07-06) and
# has no delivery track record here — the alert channel had silently never
# delivered an email. Manual test: dispatch this workflow (sends a TEST alert). Closes the gap that hid bug #1077 for 3 days (the freshness
# watchdog quietly went down Friday night and we noticed Tuesday).
#
# How it works:
#   1. workflow_run trigger listens for completion events on every scheduled
#      workflow we care about (listed under `workflows:` below).
#   2. The `if:` filter only proceeds when conclusion == 'failure'.
#   3. A small inline Python step posts a Resend email with the workflow name,
#      run URL, head sha, and head commit message.
#
# To add a new workflow to the watchlist: append its `name:` value to the
# workflows: list. The trigger is matched on workflow NAME, not file name.

on:
  workflow_run:
    workflows:
      # Daily indicator + composite chain
      - INDICATOR-REFRESH_7AM_WEEKDAYS
      # Intraday scanner triplet
      - TICKER_EVENTS_3X_WEEKDAYS
      - UNIVERSE_SNAPSHOT_3X_WEEKDAYS
      - SCAN_330PM_WEEKDAYS
      # Weekly + quarterly research
      # Massive (Polygon) market data — added Phase 3 PR #11a (2026-04-30)
      # closes the gap that left these silent on failure.
      - MASSIVE-DAILY
      - MASSIVE-INITIAL-BACKFILL
      - MASSIVE-TICKER-REFERENCE-BACKFILL
      # Other production data feeds — added Phase 3 PR #11a (2026-04-30)
      - EARNINGS_HISTORY_WEEKLY
      - SPY_SECTOR_WEIGHTS_DAILY
      - REFRESH-CONGRESS-ROSTER
      # Phase 4 cleanup PR η (2026-05-02) — close the workflow-alert watchlist
      # gap. Each of these was missing on-failure alerting.
      - DAILY-HOME-SMOKE
      - NEWS-ZH_PREMIUM_COOKIE_HEALTH-WEEKLY
      - MASSIVE-V9-ETF-BACKFILL
      - SCREENER_TRADING_OPPS_DAILY
      - PAPER-PORTFOLIO-EOD-DAILY
      - MARKET-INDICATORS-EOD
    types:
      - completed
  workflow_dispatch: {}   # manual TEST send — proves the channel end-to-end

jobs:
  alert:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'failure' }}
    runs-on: ubuntu-latest
    timeout-minutes: 3
    permissions:
      contents: read
      actions: read
    steps:
      - name: Was this a real failure or a GitHub runner-shortage cancel?
        if: github.event_name == 'workflow_run'
        # 2026-05-06 (Joe directive — "why is my data refreshing always failing,
        # I get dozens of these emails"). GitHub Actions runner shortages cause
        # workflows to finish with conclusion=failure even when no code failed —
        # the job sits in "Waiting for a runner" until GH gives up and marks
        # everything cancelled. Each of those false-positive cancellations was
        # firing this alert. New rule: fetch the run's job-level conclusions;
        # only send the email if at least one job has conclusion=failure (i.e.
        # an actual step blew up). All-cancelled => GH infra noise, suppress.
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WF_RUN_ID: ${{ github.event.workflow_run.id }}
          WF_NAME:   ${{ github.event.workflow_run.name }}
        run: |
          # Pull job-level conclusions for the failed run.
          JOBS_JSON=$(curl -fsS --max-time 10             -H "Authorization: Bearer $GH_TOKEN"             -H "Accept: application/vnd.github+json"             "https://api.github.com/repos/${{ github.repository }}/actions/runs/${WF_RUN_ID}/jobs?per_page=100")
          ANY_FAILURE=$(echo "$JOBS_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); print('true' if any((j.get('conclusion')=='failure') for j in d.get('jobs',[])) else 'false')")
          ALL_CANCELLED=$(echo "$JOBS_JSON" | python3 -c "import sys,json; d=json.load(sys.stdin); j=d.get('jobs',[]); print('true' if (j and all(x.get('conclusion')=='cancelled' for x in j)) else 'false')")
          echo "Workflow: $WF_NAME  ·  any-job-failure=$ANY_FAILURE  ·  all-jobs-cancelled=$ALL_CANCELLED"
          if [ "$ANY_FAILURE" = "true" ]; then
            echo "::notice::Real failure — alert will be sent."
            echo "IS_REAL_FAILURE=true" >> "$GITHUB_ENV"
          elif [ "$ALL_CANCELLED" = "true" ]; then
            echo "::notice::All jobs cancelled — likely GitHub runner-shortage. Suppressing alert."
            echo "IS_REAL_FAILURE=false" >> "$GITHUB_ENV"
          else
            # No failed jobs but also not all-cancelled (e.g. a mix of skipped and timed-out).
            # Default to alerting so we err toward visibility on edge cases.
            echo "::notice::Ambiguous outcome — alerting to be safe."
            echo "IS_REAL_FAILURE=true" >> "$GITHUB_ENV"
          fi

      - name: Send alert email (Gmail SMTP — same channel as the daily brief)
        if: env.IS_REAL_FAILURE == 'true' || github.event_name == 'workflow_dispatch'
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          EMAIL_TO: ${{ secrets.EMAIL_TO }}
          IS_TEST: ${{ github.event_name == 'workflow_dispatch' }}
          WF_NAME: ${{ github.event.workflow_run.name }}
          WF_RUN_URL: ${{ github.event.workflow_run.html_url }}
          WF_RUN_ID: ${{ github.event.workflow_run.id }}
          WF_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          WF_HEAD_MSG: ${{ github.event.workflow_run.head_commit.message }}
          WF_CREATED: ${{ github.event.workflow_run.created_at }}
        run: |
          python3 - <<'PY'
          import os

          name      = os.environ["WF_NAME"]
          run_url   = os.environ["WF_RUN_URL"]
          run_id    = os.environ["WF_RUN_ID"]
          head_sha  = os.environ["WF_HEAD_SHA"][:8]
          head_msg  = os.environ.get("WF_HEAD_MSG", "")[:200]
          created   = os.environ.get("WF_CREATED", "")
          if os.environ.get("IS_TEST") == "true":
              name, run_url, run_id = "TEST — alert channel verification", "https://github.com/jmezzadri/market-dashboard/actions", "manual"
              head_sha, head_msg, created = "-", "Manual dispatch to prove the failure-alert channel delivers.", "-"

          subject = f"[MacroTilt] Workflow FAILED: {name}"
          html = f"""
          <div style='font-family:-apple-system,Segoe UI,sans-serif;color:#111;line-height:1.45'>
            <h2 style='margin:0 0 8px;color:#9b2226'>Workflow failed: {name}</h2>
            <p>A scheduled GitHub Actions workflow finished with conclusion <b>failure</b>.
            Open the run log to see which step blew up.</p>
            <table style='border-collapse:collapse;font-size:13px;margin:14px 0'>
              <tr><td style='padding:4px 12px 4px 0;color:#666'>Workflow</td><td>{name}</td></tr>
              <tr><td style='padding:4px 12px 4px 0;color:#666'>Run</td><td><a href='{run_url}'>{run_id}</a></td></tr>
              <tr><td style='padding:4px 12px 4px 0;color:#666'>Started</td><td>{created}</td></tr>
              <tr><td style='padding:4px 12px 4px 0;color:#666'>Head SHA</td><td><code>{head_sha}</code></td></tr>
              <tr><td style='padding:4px 12px 4px 0;color:#666'>Head commit</td><td style='font-size:12px;color:#444'>{head_msg}</td></tr>
            </table>
            <p style='font-size:12px;color:#888'>Sent automatically by .github/workflows/WORKFLOW_FAILURE_ALERT.yml</p>
          </div>
          """

          import smtplib
          from email.mime.text import MIMEText

          user = os.environ["SMTP_USER"]
          pwd  = os.environ["SMTP_PASSWORD"]
          to   = [a.strip() for a in (os.environ.get("EMAIL_TO") or "josephmezzadri@gmail.com").split(",") if a.strip()]

          msg = MIMEText(html, "html")
          msg["Subject"] = subject
          msg["From"]    = f"MacroTilt Watchdog <{user}>"
          msg["To"]      = ", ".join(to)
          try:
              with smtplib.SMTP_SSL("smtp.gmail.com", 465, timeout=20) as s:
                  s.login(user, pwd)
                  s.sendmail(user, to, msg.as_string())
              print("SMTP send OK ->", to)
          except Exception as e:
              print("SMTP send FAILED:", e)
              raise SystemExit(1)
          PY
