# Secrets — never commit
.env
token.json

# Generated output — too noisy for git history
reports/*
!reports/latest_report.html
!reports/latest_scan_data.json

# Python artifacts
__pycache__/
*.py[cod]
.Python
*.egg-info/
.venv/
venv/
.pytest_cache/
dist/
build/

# Screener price-history cache (runtime-generated, persisted via Actions cache)
scanner/trading_opps/data/
