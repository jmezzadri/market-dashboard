import React from 'react';
import Stub from './_Stub';

export default function PortfolioPage() {
  return (
    <Stub
      eyebrow="Portfolio insights"
      title={{ before: 'Six accounts, one ', after: '.' }}
      accent="picture"
      deck="Account cards that drill inline · allocation across account / sector / asset class · positions with cost-basis P/L · CSV import (Chase, Fidelity, Schwab). Plaid coming soon."
    />
  );
}
