/* HomePage stub — real composition lands in PR-O10. */

import React from 'react';
import Stub from './_Stub';

export default function HomePage() {
  return (
    <Stub
      eyebrow="Today · May 26, 2026"
      title={{ before: 'The regime reads ', after: '.' }}
      accent="risk-on inflationary"
      deck="Editorial hero, two-column regime read, three feature cards, market news list — built on top of the finished Macro / Tilt / Indicators components."
    />
  );
}
