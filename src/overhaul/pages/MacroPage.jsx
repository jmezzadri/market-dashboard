import React from 'react';
import Stub from './_Stub';

export default function MacroPage() {
  return (
    <Stub
      eyebrow="Macro overview"
      title={{ before: 'Twenty-seven inputs say the cycle is ', after: '.' }}
      accent="late"
      deck="Domain strip · regime canvas with all indicators positioned by state and domain · grid view alternative · click any indicator to drill in place."
    />
  );
}
