import React from 'react';
import Stub from './_Stub';

export default function IndicatorsPage() {
  return (
    <Stub
      eyebrow="All indicators"
      title={{ before: 'Every input, in ', after: '.' }}
      accent="one table"
      deck="Layer + category filters · sortable table with current, 3 / 6 / 12-month history, five-year sparkline · click row to drill inline."
    />
  );
}
