import React from 'react';
import Stub from './_Stub';

export default function ScannerPage() {
  return (
    <Stub
      eyebrow="Trading scanner"
      title={{ before: 'Long signals across three score ', after: '.' }}
      accent="buckets"
      deck="Score-bucket pills · column picker · scoring methodology callout · per-row drill with the score math table that reconciles to the headline and the 90-day chart with event markers."
    />
  );
}
