import React from 'react';
import Stub from './_Stub';

export default function MethodologyPage() {
  return (
    <Stub
      eyebrow="Methodology"
      title={{ before: 'How the ', after: ' actually works.' }}
      accent="engine"
      deck="Editorial long-form across macro, tilt, scanner, portfolio, scenarios, freshness, sources, and changelog. Formula blocks in mono, vendor table, deep-link anchors."
    />
  );
}
