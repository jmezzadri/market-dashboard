# x-charts branch

Delivery channel for the MacroTilt Daily X Chart. The cloud chart producer
pushes `chart.png` + `caption.txt` here each weekday morning; the
X-CHART-EMAIL workflow emails both to Joe as an attachment. This branch is
intentionally separate from main and never merges into it.
